[build]
command = "npm install && npm run build"
publish = "dist"

[build.environment]
NODE_VERSION = "20"
NPM_FLAGS = "--prefer-offline --no-audit --no-fund --platform"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200